{
  "title": "Enigma2 MQTT",
  "config": {
    "flow_title": "{name}",
    "step": {
      "confirm": {
        "title": "Add this Enigma2 receiver",
        "description": "Enigma2 box {name} ({boxtype}, {image}) has announced itself on the broker.\n\nAdding it switches the box into integration mode: the plugin withdraws the MQTT discovery entities it publishes on its own, and Home Assistant builds the device instead. The box has to confirm the switch before it is added."
      },
      "user": {
        "title": "Add an Enigma2 receiver by hand",
        "description": "Use this when the receiver's announcement never reached Home Assistant, for example because the box publishes through an MQTT bridge that rewrites the topic prefix.",
        "data": {
          "base_topic": "Base topic",
          "node_id": "Node ID",
          "name": "Name"
        },
        "data_description": {
          "base_topic": "The prefix the receiver publishes under, as Home Assistant sees it on the broker. The plugin's default is `enigma2`.",
          "node_id": "The receiver's node ID, shown on the plugin's setup screen. It is the box type and the last six digits of the MAC address, for example `vuuno4kse_005301`.",
          "name": "What to call this receiver in Home Assistant. Left empty, the name the plugin reports is used."
        }
      }
    },
    "error": {
      "no_ack": "The receiver did not confirm the mode switch. Check that the plugin is running and that its broker login may write to this node's topics.",
      "not_found": "No receiver was found on this topic. Check the base topic and the node ID on the plugin's setup screen.",
      "invalid_topic": "That is not a usable topic. The base topic and the node ID must not contain the `+` or `#` wildcards."
    },
    "abort": {
      "already_configured": "Device is already configured",
      "already_in_progress": "This receiver is already being added.",
      "mqtt_unavailable": "The MQTT integration is not available. Set it up first, then add the receiver.",
      "not_enigma2_announcement": "That message is not an Enigma2 MQTT Bridge announcement.",
      "retracted": "The receiver has withdrawn its announcement."
    }
  },
  "exceptions": {
    "mqtt_not_available": {
      "message": "The MQTT integration is not available, so the receiver cannot be reached."
    }
  }
}
